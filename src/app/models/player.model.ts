/*
 * Represents a player
 * 
 */
export class PlayerModel {

    // Player nickname
    public name: string;

    // Total count of finished games
    public gamesCount: number;

    // Best score (minimal steps to finish the game)
    public bestScore: number;
}
